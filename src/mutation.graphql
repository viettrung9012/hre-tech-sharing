type Mutation {
    addTask(
        summary: String!
        dueDate: String
        category: String
        priority: Int
    ): Task

    updateTask(
        id: Int!
        summary: String
        dueDate: String
        category: String
        priority: Int
    ): Task

    deleteTask(id: Int!): String
}